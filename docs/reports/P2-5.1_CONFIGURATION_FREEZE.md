# P2-5.1 EXP-001 Configuration Freeze

> Date: 2026-09-22
>
> CONFIGURATION FREEZE: COMPLETE
>
> TRAINING AUTHORIZATION: NOT GRANTED
>
> Training execution: NOT STARTED
>
> Dataset modification: NONE
>
> Mapping modification: NONE

## Scope

This record freezes the canonical `EXP-001` training configuration. It does
not download starting weights, install dependencies, train a model, create a
checkpoint, or modify the frozen dataset, mapping, or fingerprints.

Canonical configuration:

```text
configs/training/exp001_baseline.yaml
```

The canonical configuration remains `execution_enabled: false`. Configuration
freeze is not training authorization.

## Frozen Training Parameters

| Parameter | Frozen value |
| --- | --- |
| Experiment | `EXP-001` |
| Model | `YOLO11n` |
| Model implementation version | Ultralytics `8.4.157` |
| Starting weights | `yolo11n.pt` |
| Weight provenance | Ultralytics official pretrained YOLO11n checkpoint |
| Dataset | `CSS-PPE-10-V1` |
| Dataset contract | `docs/dataset_contracts/CSS-PPE-10-V1-TRAINING.yaml` |
| Processed dataset | `data/processed/css-ppe-10-v1/` |
| Class count | `7` |
| Class order | `person`, `hardhat`, `no_hardhat`, `vest`, `no_vest`, `machinery`, `vehicle` |
| Image size | `640` |
| Epochs | `100` |
| Batch size | `16` |
| Optimizer | `AdamW` |
| Initial learning rate | `0.001` |
| LR strategy | `cosine` |
| Final LR fraction | `0.01` |
| Weight decay | `0.0005` |
| Warmup epochs | `3.0` |
| Early-stopping patience | `20` |
| Seed | `42` |
| Device | `cuda:0` |
| Device strategy | Single AutoDL RTX 4090 24GB |
| Workers | `8` |
| Deterministic | `true` |
| AMP | `true` |
| Cache | `false` |
| Output path | `experiments/runs/EXP-001` |
| Logs path | `artifacts/logs/EXP-001` |
| Reports path | `experiments/reports/EXP-001` |
| Checkpoint path | `models/checkpoints/EXP-001` |

The starting-weight reference is frozen without downloading the binary. Its
SHA256 must be captured before execution once weight acquisition is explicitly
authorized.

## Frozen Augmentation

Configuration:
`experiments/configs/augmentation.yaml`

| Parameter | Frozen value |
| --- | --- |
| Enabled | `true` |
| Pipeline | `ultralytics_yolo11` |
| Image scale | `0.5` |
| Translation | `0.1` |
| Horizontal flip | `0.5` |
| Color jitter | `0.2` |
| Mosaic | `1.0` |
| Close mosaic | `10` |

## Configuration Fingerprints

| File | SHA256 |
| --- | --- |
| `configs/training/exp001_baseline.yaml` | `df6c55ae6deb4697493a2c1e49ac171bff5c38edfd73ad968aa5e1e2cacff989` |
| `configs/training/schema.yaml` | `909457210c97e1139df978a5f89d50436cfdb2e22577696a930236b3a24c2477` |
| `experiments/configs/augmentation.yaml` | `bb925382ff9deba525d57eced7762326d8bfb7ae82f49390c42bfa81b6f29f59` |
| `experiments/configs/baseline.yaml` | `206308e7c0bfcdf83aa6e50bc531a18d6ef155adb82c116e7b4980491937165b` |

These hashes identify the reviewed file bytes at the completion of P2-5.1.

## Frozen Dataset Identity

| Item | Frozen value |
| --- | --- |
| Dataset ID | `CSS-PPE-10-V1` |
| Mapping | `PPE-MAPPING-V1` |
| Source `data.yaml` SHA256 | `5c393e74086c366a2ef55a77a4ddbf26bde1e08887a16f292f30a69fb3d21b34` |
| Source manifest SHA256 | `ea0de4b0ca379c5aae066e500b1e0bf30b69ae71467c2bb99e4f2cbd5f98d795` |
| Processed manifest SHA256 | `dbfe43c43d45e65951abec1bd112e07342b66651d799a05d79b6d749f2831c2c` |

The dataset and mapping were not modified by this freeze.

## Immutability Rule

An executed run must use the canonical configuration unmodified. Any change to
one of the following creates a new configuration revision or experiment ID:

- model version or starting weights;
- dataset or mapping identity;
- image size, epochs, batch, optimizer, learning-rate strategy, or seed;
- augmentation settings;
- device or worker strategy; or
- output paths.

## Pre-Execution Gates

The following remain outside the configuration freeze and must be satisfied
before training:

1. explicit human training authorization;
2. acquisition and SHA256 verification of `yolo11n.pt`;
3. complete dependency lock/environment freeze approval; and
4. execution-time verification of dataset and configuration fingerprints.

## Decision

`P2-5.1 CONFIGURATION FREEZE: COMPLETE`

`TRAINING: NOT AUTHORIZED`

## P2-5.2 Registration Update

P2-5.2 subsequently acquired the frozen `yolo11n.pt` initialization weight
from the official Ultralytics `v8.3.0` asset release and recorded:

```text
local path: models/pretrained/yolo11n.pt
size:       5613764 bytes
sha256:     0ebbc80d4a7680d14987a577cd21342b65ecfd94632bd9a8da63ae6417644ee1
manifest:   docs/weights/EXP-001_WEIGHT_MANIFEST.yaml
```

This resolves the local binary acquisition and checksum item. Remote transfer
and complete environment-freeze approval remain pre-execution gates. It does
not change the configuration hashes above or grant training authorization.

## P2-5.3 Dependency Freeze Update

P2-5.3 subsequently froze the resolved AutoDL environment and recorded:

```text
conda lock:   locks/EXP-001/conda-environment.yml
explicit:     locks/EXP-001/conda-explicit.lock
pip lock:     locks/EXP-001/pip-freeze-all.txt
fingerprint:  locks/EXP-001/runtime-fingerprint.yaml
```

The environment remains `ppe-exp001`, Python `3.10.21`, PyTorch
`2.5.1+cu124`, Ultralytics `8.4.157`, RTX 4090 with driver `560.35.03`, and
PyTorch CUDA runtime `12.4`. This resolves the complete dependency-lock gate
without changing the canonical configuration hashes. Remote weight transfer
and explicit human training authorization remain outstanding.
