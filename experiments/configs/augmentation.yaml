schema_version: training-config-v1
status: CONFIGURATION_FROZEN
execution_enabled: false

augmentation:
  enabled: true
  pipeline: ultralytics_yolo11
  parameters:
    image_scale: 0.5
    translation: 0.1
    horizontal_flip: 0.5
    color_jitter: 0.2
    mosaic: 1.0
    close_mosaic: 10

notes:
  - Values are frozen for EXP-001 by P2-5.1.
  - No augmentation or training is executed by this configuration freeze.
